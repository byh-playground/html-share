# 변경 기록

## 0.1.0 — 공개 베타 준비

### 파일 공유와 버전 관리

- 프로젝트별로 수정일이 가장 최근인 HTML·HTM·ZIP을 제공합니다. ZIP은 경로와 크기 등을 검증한 뒤 압축을 풉니다.
- 관리 링크의 인증 키를 유지하면서 파일 업로드, 적용 버전 선택, 삭제 미리보기와 이전 파일 정리를 지원합니다.
- 각 버전의 원본 다운로드를 추가했습니다. PWA 가공본 대신 업로드 원본을 받으며, 다운로드해도 적용 버전과 수정일은 바뀌지 않습니다.

### 모바일 관리 화면

- 업로드·적용 관리·PWA 설정·공유의 네 개 탭과 카드형 화면을 구성했습니다. 탭 이동 시 선택 파일과 미저장 설정을 유지하고, 최근 탭 복원과 키보드 탐색을 지원합니다.
- 업로드 후 현재 탭에서 프로젝트를 바로 열고, 뒤로 가면 같은 프로젝트의 관리 화면으로 돌아오는 흐름을 추가했습니다. 기존 업로드만 하기와 새 탭 열기도 유지합니다.
- 현재 적용 파일·빌드 요약, 업로드 완료 카드, 다음 수정본 선택 기능을 제공합니다.
- 공유 탭에서 방문자용 페이지 공유와 본인 기기 연결을 구분합니다. 관리 QR과 링크는 기존 키를 재사용하고, 대화상자를 닫으면 화면에서 지웁니다.

### 외부 접속과 알림

- QR을 통한 첫 휴대폰 연결과 ngrok 계정·토큰의 최초 설정 절차를 한글로 안내합니다.
- `start -Provider cloudflare`로 계정 없이 Cloudflare Quick Tunnel을 사용할 수 있습니다. 선택한 제공자는 다음 `start`에 재사용하며, `start -Provider ngrok`으로 복귀할 수 있습니다.
- Quick Tunnel 프로세스가 종료되면 백그라운드에서 다시 실행합니다. ntfy가 활성화되어 있으면 새 주소를 알리고 전송 실패 시 재시도합니다. 프로세스가 유지되는 동안 같은 주소로 연결만 복구되면 알림을 반복하지 않습니다.
- 터널 제공자를 바꿔도 로컬 서버·프로젝트·관리 키는 유지합니다. 도메인이 바뀔 때의 관리 링크·QR·PWA 사용법을 안내합니다.
- ntfy는 선택 기능입니다. 관리 링크 다시 받기, 프로젝트 링크 수동 전송, Android 구독 QR을 제공합니다. 업로드·재적용·PWA 설정 저장의 자동 알림은 별도로 켤 수 있습니다.
- ntfy를 끄더라도 관리 페이지는 계속 사용할 수 있습니다. ntfy 미설정 상태에서 불필요한 실패 재시도를 반복하던 문제를 수정했습니다.

### PWA와 관리 보안

- 공용 PWA 템플릿에 프로젝트별 메타데이터·배포본·오프라인 캐시를 적용하고 업로드 원본을 보존합니다.
- 통합 관리 화면을 공개 프로젝트 밖으로 분리하고, 기존 키에서 계산하는 고정 비공개 경로에서 제공합니다. 공개 루트에는 프로젝트 링크만 표시합니다.
- 기존 관리 경로와 추측 가능한 경로는 404로 응답하며, 관리 API는 별도 인증을 요구합니다. 비공개 관리 QR과 공개 프로젝트 QR을 구분합니다.
- 임의 프로젝트의 서비스 워커 등록을 차단했습니다. 이는 프로젝트 간 완전한 격리를 제공하는 기능은 아닙니다.

### 저장소와 검증

- 서비스 코드는 `src/`, 실행 도구는 `scripts/`, 테스트는 `tests/` 아래에 역할별로 정리했습니다. 기존 실행 명령과 개인 데이터 경로는 유지합니다.
- README는 저장소 정보 중심으로 정리하고, 한글 HTML 사용 설명서와 HTML Preview 링크를 추가했습니다.
- 공백 4칸 들여쓰기, EditorConfig·Prettier 설정, 포맷·검사 명령을 제공합니다.
- npm 잠금 파일, Windows 설치·환경 확인 명령, 공개 소스 내보내기 허용 목록과 GitHub Actions 검사를 구성했습니다.
- ZIP 항목을 전부 읽기 전에 개수 제한을 검사하도록 수정했습니다. 프로젝트 전환 중 미저장 설정이 다른 프로젝트에 적용되는 문제, ZIP의 PWA 전환, JavaScript 빌드 ID 판별도 보완했습니다.
- `local` 명령이 기존 공개 터널을 유지한다는 점과 `stop` 후 로컬 전용으로 시작하는 절차를 명확히 했습니다.

지원 범위는 소유자 한 명이 신뢰하는 파일을 미리 보고 공유하는 개인용 도구입니다. 다중 사용자 격리나 상시 운영용 호스팅을 보장하지 않습니다.
