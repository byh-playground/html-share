# 보안 및 지원 범위

HTML Share는 **소유자 한 명이 신뢰하는 HTML·ZIP 파일을 공유하는 도구**입니다. 사용자 간 격리를 제공하는 다중 사용자 호스팅이 아니며, 익명 사용자의 파일이나 신뢰하지 않는 코드를 받아서는 안 됩니다.

## 신뢰 범위와 프로젝트 간 관계

- 모든 프로젝트는 같은 웹 출처(origin)를 사용합니다. 프로젝트의 JavaScript는 같은 출처의 브라우저 저장소와 기능에 접근할 수 있습니다. 프로젝트 경로를 나누는 것만으로 보안 격리가 이루어지지는 않습니다.
- 임의 프로젝트의 서비스 워커 등록은 차단합니다. PWA 옵션을 켜서 생성된 공용 서비스 워커를 사용하세요. 생성된 워커의 요청 처리와 캐시 삭제 범위는 해당 프로젝트로 제한합니다.
- 이 제한은 업그레이드 전에 설치된 서비스 워커를 제거하지 않으며, 악성 HTML을 안전하게 만들지도 않습니다. 신뢰하는 프로젝트에 전용 출처를 사용하세요. 신뢰하지 않는 작성자를 지원하려면 관리 화면과 각 프로젝트의 출처를 분리해야 합니다.
- 로컬 템플릿·설정·파일시스템 접근은 관리 권한으로 취급합니다. 런타임 파일이나 실행 코드를 수정할 수 있는 로컬 사용자는 애플리케이션의 접근 제어를 우회할 수 있습니다.

## 업로드와 관리 인증

공개 루트는 프로젝트 목록만 제공합니다. 관리 화면은 `public` 밖의 `src/management/ui/`에 보관하며, 기존 키에서 계산하는 고정 비공개 경로에서만 제공합니다. 공개 페이지·프로젝트 링크·오류 응답·프로젝트 공유 QR에는 관리 경로를 노출하지 않습니다. 기존 `/upload` 등 추측 가능한 관리 경로는 404로 응답합니다. 경로를 알더라도 API에는 별도 인증 토큰이 필요합니다. 주소를 숨기는 것은 발견을 어렵게 하는 장치이며, 인증이나 출처 분리를 대신하지 않습니다.

관리 QR은 비공개 관리 링크를 포함하므로 소유자만 사용해야 합니다. 프로젝트 QR에는 관리 경로나 키 없이 공개 페이지 주소만 포함됩니다. QR은 로컬에서 생성하며 외부 QR 생성 서비스에 관리 링크를 보내지 않습니다.

관리 URL의 `#` 뒤에는 지속적으로 사용하는 256비트 인증 키가 포함됩니다. 링크를 가진 사람은 파일 업로드·원본 다운로드·PWA 설정 변경·버전 적용·이전 원본 정리를 할 수 있습니다. `#` 뒤의 값은 일반 HTTP 요청에 포함되지 않지만, 같은 출처에서 충분한 접근 권한을 가진 브라우저 코드가 읽을 수 있습니다. 비공개로 보관하세요.

API는 `Authorization: Bearer` 토큰을 요구하고 브라우저의 `Origin` 헤더를 검사합니다. 로그인 쿠키는 사용하지 않으며 임의 사이트에 CORS 접근을 허용하지 않습니다. 토큰을 브라우저의 `localStorage`나 `sessionStorage`에 저장하지 않습니다. 관리 링크가 담긴 스크린샷·방문 기록·알림을 공유하지 마세요.

ntfy의 기본 토픽은 무작위로 생성하지만 **접근 제어나 종단 간 암호화를 제공하는 채널은 아닙니다.** 휴대폰 알림은 사용자가 설정한 경우에만 사용하며, 알림 서비스가 관리 링크를 처리할 수 있습니다. 민감한 다중 사용자 관리 용도로 사용하지 마세요.

키가 노출되었다면 서버 실행 중 `.\share.ps1 manage -Rotate`를 실행하고 새 관리 링크를 사용하세요. 이전 경로와 키는 사용할 수 없게 됩니다. 일반적인 재시작은 키를 교체하지 않습니다. 환경 변수 `HTML_SHARE_UPLOAD_TOKEN`으로 키를 지정했다면 해당 값을 교체하고 재시작해야 합니다.

## 파일 처리와 자원 사용

- 파일명·프로젝트명·숨김 경로·심볼릭 링크·ZIP 경로·항목 수·크기 제한을 검사합니다.
- 업로드가 완료된 파일만 게시합니다. 이름이 중복되면 새 이름을 부여하며 기존 파일을 임의로 덮어쓰지 않습니다.
- 원본 다운로드는 인증된 요청과 현재 파일 목록을 확인한 뒤 원본 파일을 첨부 파일로 전송합니다. 다운로드가 적용 버전이나 수정일을 바꾸지는 않습니다.
- 정리 작업은 인증된 명시적 요청과 현재 파일 목록의 버전 확인이 필요합니다. 해당 폴더의 HTML·HTM·ZIP 원본만 대상으로 하며 관련 자원과 프로젝트 설정은 보존합니다.
- 업로드 파일을 원자적으로 게시하려면 런타임과 공개 저장소가 같은 파일시스템에 있어야 합니다.
- 제한은 개별 요청에 적용됩니다. 사용자별 할당량, 포괄적인 요청 빈도 제한, 바이러스 검사, 여러 프로세스 간 작업 조정은 제공하지 않습니다. 검증을 통과한 큰 파일과 PWA 생성도 CPU·메모리·디스크를 사용합니다.
- 이전 ZIP 스냅샷과 PWA 배포본은 보존합니다. 캐시를 수동 정리하기 전에 서비스를 중지하고, 원본·PWA 설정·인증 파일은 백업하세요.

## 저장소에 올리지 않는 정보

루트의 `.runtime/`, `.tools/`, `node_modules/`, `projects/`와 예제 외 공개 프로젝트 데이터는 Git에서 제외합니다. 소스 내보내기도 허용 목록을 사용합니다. 비공개 파일을 강제로 Git에 추가하거나 소스 예시에 인증정보를 복사하지 마세요.

공개 저장소에 문제를 보고할 때 실제 키나 개인 파일을 포함하지 마세요. 테스트용 파일로 재현하고 로그·URL에서 인증정보를 제거한 뒤 공유하세요.
